version: "3.8"

services:
  web-teduhkostmatahari:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teduhkostmatahari-app
    ports:
      - "127.0.0.1:3003:3000"
    restart: unless-stopped
